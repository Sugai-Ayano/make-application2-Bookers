<!--Booksのshowページ-->
<main>
        <p id="notice"></p>
        <div class='container'>
  <div class='row'>
 <%= render 'users/sidemenu'%>
    <div class='col-md-8 offset-md-1'>
  		<h2>Book detail</h2>
    <tr>
        <% @books.each do |book| %>
         <td><%= book.title %></td>
         <td><%= link_to "title", book_path(book.id) %></td>
    </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>
  </main>
  <!--Booksのshowページ-->